# App name and server port (optional)
spring.application.name=userDatabase
server.port=3001

# View config (for JSP)
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp

# MySQL database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/sample?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Rosie3175@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Hibernate auto-setup
spring.jpa.hibernate.ddl-auto=update


# Optional: Enable JSP development mode
server.servlet.jsp.init-parameters.development=true